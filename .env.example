# Configuration contract. Mirrors 15_Environment_Variables.example, adjusted for the stack
# recorded in docs/SPEC_AMENDMENTS.md (ADR-AMEND-A, ADR-AMEND-B).
#
# Every value is validated by packages/config at process start. A CHANGE_ME left in place is
# rejected outright rather than warned about, so boot fails loudly instead of running with a
# predictable session secret.

NODE_ENV=development

# App
APP_BASE_URL=http://localhost:3000
API_BASE_URL=http://localhost:3000/api/v1
SESSION_COOKIE_NAME=dh_session
SESSION_SECRET=
APP_ENCRYPTION_KEY=
HASH_PEPPER=

# Database
# Dev/CI: a Neon branch (ap-south-1 Mumbai). Staging/production: DigitalOcean Managed
# Postgres (BLR1). Both are stock Postgres 17+; nothing here requires 18 (ADR-AMEND-C).
DATABASE_URL=postgresql://user:pass@localhost:5432/ai_review
DATABASE_POOL_MIN=2
# 2 web containers + 1 worker x 20 = 60 connections. Confirm this fits the instance tier.
DATABASE_POOL_MAX=20

# Redis / queues
REDIS_URL=redis://localhost:6379

# OpenAI
# Model and prompt are admin-versioned in ai_prompt_versions (ADR-006); these only bootstrap.
# Luna verified at $0.20/M in, $1.20/M out — about 4.6% of revenue at scale.
# Terra is 10x. Never wire it as an automatic failover; see docs/SPEC_AMENDMENTS.md.
OPENAI_API_KEY=
OPENAI_DEFAULT_MODEL=gpt-5.6-luna
OPENAI_FALLBACK_MODEL=gpt-5.6-terra
OPENAI_REASONING_EFFORT=none
AI_MAX_OUTPUT_TOKENS=220
AI_REQUEST_TIMEOUT_MS=8000

# Razorpay — not required until E10
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
RAZORPAY_ANNUAL_PLAN_ID=

# Cloudflare — WAF/CDN, R2, and Cloudflare for SaaS custom hostnames. Not required until E11.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_SAAS_FALLBACK_ORIGIN=

# Object storage — Cloudflare R2 via the S3-compatible API
S3_ENDPOINT=
S3_REGION=ap-south-1
S3_BUCKET=ai-review-assets
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_PUBLIC_BASE_URL=

# Email
EMAIL_FROM=no-reply@digitalhammerr.com
SES_REGION=ap-south-1

# Observability
SENTRY_DSN=
OTEL_EXPORTER_OTLP_ENDPOINT=
LOG_LEVEL=info

# Bootstrap defaults. platform_settings in the database takes over once seeded (ADMIN-04).
FREE_AI_GENERATION_LIMIT=10
PRO_ANNUAL_PRICE_PAISE=99900
DEFAULT_TIMEZONE=Asia/Kolkata
