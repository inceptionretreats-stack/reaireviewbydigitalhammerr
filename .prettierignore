node_modules/
dist/
.next/
coverage/
pnpm-lock.yaml
drizzle/

# The delivered spec pack is a frozen contract — never reformat it.
docs/spec/

# Generated output — the generator is canonical, and CI verifies it by regenerating.
*.generated.ts

# Regenerated by `next dev`/`next build` with Next's own quote style, and the file says not to edit
# it. Formatting it means every dev-server start breaks `pnpm format:check`.
apps/web/next-env.d.ts
